# MoviePilot-Plugins

个人维护的 MoviePilot 插件仓库，建议与官方仓库一起使用：
- [jxxghp/MoviePilot-Plugins](https://github.com/jxxghp/MoviePilot-Plugins)

仓库地址：
- [lucku88/MoviePilot-Plugins](https://github.com/lucku88/MoviePilot-Plugins/)

## 插件列表

| 插件 | 版本 | 说明 | 标签 |
| --- | --- | --- | --- |
| `Vue-农场` | `v0.2.19` | 动态收菜、种植、出售、分时偷菜、批量点赞、可视化农场操作 | 站点 |
| `Vue-魔丸` | `v0.2.18` | 动态搬砖、清沙滩、炼造兑换、单件/批量赠送与赠礼统计 | 站点 |
| `Vue-玩偶` | `v0.2.11` | 盲盒、自己展位优先、动态收回与外展 | 站点 |
| `Vue-表情` | `v0.2.3` | 老虎机、开包、舞台演出、网页操作日志、自动挖角 | 站点 |
| `Vue-面板` | `v0.1.39` | 个人用模块化面板。 | 站点 |
| `飞牛影视媒体库封面生成` | `v0.1.14` | 生成静态封面并回写飞牛影视 | 媒体服务器 |

## 插件说明

### 🌱 Vue-农场
- `v0.2.19` 修复 MoviePilot 容器更新后后台插件同步会再次初始化插件，Vue-农场停止旧实例时误删全局动态任务的问题；现在只清理插件自己的临时任务，由 MoviePilot 统一替换总调度任务，即使后续同步流程中断也不会让收菜任务消失。小版本升级继续保留配置、Cookie、执行历史和动态调度计划。
- `v0.2.18` 修复 MoviePilot 启动时插件清理旧任务会提前创建总调度器、导致 Vue-农场 尚未加载完成就漏注册的问题；容器重启或更新后会正常注册初始化任务，再按真实成熟时间刷新和补跑。小版本升级继续保留配置、Cookie、执行历史和动态调度计划。
- `v0.2.17` 修复 MoviePilot 容器重启或更新后，启动初始化和动态收菜任务可能因为调度器启动较慢而被当成已经错过的问题；迟到的任务仍会恢复刷新状态，再沿用原有成熟时间和补跑规则，不改成固定周期。小版本升级继续保留配置、Cookie、执行历史和动态调度计划。
- `v0.2.16` 修复网站已经卖出作物、但出售请求响应超时后通知漏记的问题；插件会在出售结束后对比背包数量，自动补齐已售作物和收入，避免净收益被算错。小版本升级继续保留配置、Cookie、执行历史和动态调度计划。
- `v0.2.15` Cookie 配置改为 `Vue-思齐农场` 同款紧凑密码输入框，查看和站点同步按钮都放在输入框内；插件继续自动同步 MoviePilot 的 `si-qi.xyz` 站点 Cookie，读取失败时才使用保存值作为备用。小版本升级继续保留配置、Cookie、执行历史和动态调度计划。
- `v0.2.14` 移除配置页和后端的强制 IPv4 限制，连接由系统自动选择可用的 IPv4 或 IPv6；热更新会清理旧版遗留限制，旧配置自动忽略，并继续保留 Cookie、配置、执行历史和动态调度计划。
- 状态页、配置页和主要操作直接以 [KoWming/Vue-思齐农场 v1.0.1](https://github.com/KoWming/MoviePilot-Plugins/releases/tag/SiqiFram_v1.0.1) 的源码为底稿移植，再接入本仓库原有的稳定自动化功能。
- 收菜继续使用动态调度：识别每块田的真实成熟时间，并在成熟后增加缓冲秒数再运行，不使用固定 4 小时周期。
- 手动种植同样会登记动态收菜计划；成熟前预刷新后会强制注册正式收菜，即使作物临时消失也会保留原计划，到点运行并自动补种空地。
- 立即运行或页面种植后会等待新作物写入田地状态，并按该种子的生长时间保底排期；只要存在成熟田块，就会优先立即收菜，不会被其他远期作物时间跳过。
- 正式收菜时不再叠加两层网络重试；批量失败会先读取最新田地，再逐块收仍然成熟的作物，已被偷的坑位不会误报为收菜成功。
- 通过插件管理中心重装或热更新后，旧版 Vue 接口会自动转交给当前运行实例，不再用旧版本覆盖新版状态和调度。
- 支持自动收菜、优先种植、一键种植、一键出售、逐坑位操作和一行执行历史。
- 固定优先使用 MoviePilot 内置 OCR 批量收菜，不再需要选择 OCR 来源或填写外部 OCR 地址。
- OCR 失败后可选用 MoviePilot AI 辅助识别；OCR 和 AI 都失败、或批量后发现漏收时会立即逐坑位补收。整次收菜默认限制为 45 秒，并为逐坑位阶段预留时间。
- OCR 批量收菜不再提供开关，始终优先使用内置 OCR；识别次数、AI 辅助识别和收菜保护时限统一位于配置页“参数设置”。
- 优先种植会兼容带逗号的总收获数字，并信任种子已解锁状态，避免蘑菇等新解锁种子被误判为未解锁。
- 自动偷菜支持同时选择多个作物、每轮访问人数和多个时间段；随机到所选作物时会按网站当天剩余额度尽量连续偷取，没有目标时会在下一次互动检查继续尝试，网站提示次数耗尽后才停止当天任务。
- 偷菜通知按实际成功结果汇总，例如 `🌽玉米×3`，不会再把所有已选择作物都写进通知。
- 自动点赞支持三个固定用户名，未填写的位置会用不重复的随机目标补齐；执行时间使用带可读解释的 Cron 设置，默认 `0 9 * * *`（每天 09:00），错过后当天会自动补跑。
- 配置页的“运行一次”会执行主农场任务，并同步执行已开启的偷菜和点赞；旧版本保存的 JSON 格式多选作物会自动修正显示。
- 站点 Cookie 固定自动同步，无需配置开关；插件启动、保存和请求前都会读取 MoviePilot 站点管理中的最新 Cookie，手动输入仅作同步失败备用。
- 支持手动进入随机农场选择坑位偷菜、随机填充点赞名单和批量点赞；当天无点赞目标时不会反复请求。
- 网络异常单次请求最多自动重试 5 次，连续失败超过 5 轮后停止短间隔补跑，避免夜间刷屏。

### ⚗️ Vue-魔丸
- `v0.2.18` 修复 MoviePilot 容器更新后后台插件同步会再次初始化插件，Vue-魔丸停止旧实例时误删全局动态任务的问题；现在只清理插件自己的临时任务，由 MoviePilot 统一替换总调度任务，即使后续同步流程中断也不会让搬砖和沙滩任务消失。`v0.2.x` 小版本升级不会清除现有配置、Cookie、执行历史和动态调度计划。
- `v0.2.17` 修复 MoviePilot 启动时插件清理旧任务会提前创建总调度器、导致 Vue-魔丸 尚未加载完成就漏注册的问题；容器重启或更新后会正常注册初始化任务，再按搬砖 Cron 和沙滩冷却规则补跑。`v0.2.x` 小版本升级不会清除现有配置、Cookie、执行历史和动态调度计划。
- `v0.2.16` 修复 MoviePilot 容器重启或更新后，启动初始化和动态搬砖、沙滩任务可能因为调度器启动较慢而被当成已经错过的问题；迟到的任务仍会恢复刷新状态，再沿用原有搬砖 Cron 和沙滩冷却规则判断是否补跑。`v0.2.x` 小版本升级不会清除现有配置、Cookie、执行历史和动态调度计划。
- `v0.2.15` Cookie 配置改为 `Vue-思齐农场` 同款紧凑密码框，查看和站点同步按钮都放在输入框内；运行时优先读取 MoviePilot 的 `si-qi.xyz` 站点 Cookie，站点读取失败时才使用已保存值作为备用。`v0.2.x` 小版本升级不会清除现有配置、Cookie、执行历史和动态调度计划。
- `v0.2.14` 修正 Vue-魔丸 炼造工坊操作区：配方名称与数量框、炼造按钮合并到同一行，材料独立排列，去掉重复的红色上限提示；手机端按标题、材料、操作纵向排列，并继续适配浅色和深色主题。`v0.2.x` 小版本升级不会清除现有配置、Cookie、执行历史和动态调度计划。
- `v0.2.13` 微调 Vue-魔丸 炼造工坊三列配方的操作区：数量框和炼造按钮统一靠右对齐，减少卡片空白，并保持浅色、深色和移动端布局；不可赠送物品不显示赠送控件。`v0.2.x` 小版本升级不会清除现有配置、Cookie、执行历史和动态调度计划。
- `v0.2.12` 继续微调 Vue-魔丸 物品栏和炼造工坊：不可赠送的物品保留库存信息但不显示赠送控件或技术提示，炼造配方卡统一为底部操作区并优化三列布局。`v0.2.x` 小版本升级不会清除现有配置、Cookie、执行历史和动态调度计划。
- `v0.2.11` 微调兑换与炼造工坊布局，隐藏后端上限提示，缩小数量输入框并将炼造配方改为紧凑三列；赠送功能仅允许砖块、木材、塑料袋、瓶子、螺丝、旧电池、破铜片和蚯蚓，前后端同时校验，避免赠送合成物品。`v0.2.x` 小版本升级不会清除现有配置、Cookie、执行历史和动态调度计划。
- `v0.2.10` 修复网站真实赠送接口要求使用 `uid`、旧版错误发送 `target_uid` 导致“参数错误”的问题；单件和批量赠送均已修正，并继续保留批量校验、顺序赠送、失败即停和请求防重复。`v0.2.x` 小版本升级不会清除现有配置、Cookie、执行历史和动态调度计划。
- `v0.2.9` 精简兑换魔力区域说明，物品栏与炼造工坊分别占据整行；新增批量赠送，可勾选多种物品共用一个 UID。后端先完整校验再顺序赠送，部分失败时停止并报告已成功项目；同一请求编号只执行一次，网络响应丢失后重试不会重复赠送。`v0.2.x` 小版本升级保留现有配置、Cookie、执行历史和动态调度计划。
- `v0.2.8` 恢复 `v0.2.6` 经确认的状态页排版与 Vue-农场同款自适应风格；撤销 `v0.2.7` 和未发布新版的任务优先重排，同时保留后来增加的接口防报错、数据校验和后端稳定性修复。`v0.2.x` 小版本升级保留现有配置、Cookie、执行历史和动态调度计划。
- `v0.2.7` 状态页改为更容易看懂的“任务优先工作台”：打开页面后，第一屏就能看到自动任务、下一步会触发什么操作以及兑换入口；物品栏改为全宽紧凑网格，炼造工坊改为三列配方布局，并继续适配浅色、深色和手机页面。`v0.2.x` 小版本升级不会丢失现有配置、Cookie、执行历史和动态调度计划。
- `v0.2.6` 状态页直接对齐 `Vue-思齐农场 v1.0.3`，搬砖与沙滩改为同款动态任务操作卡片，去除“后端标记”技术文案并显示正常业务状态；保留魔丸、随机延迟等数字输入值已垂直居中。`v0.2.x` 小版本升级继续保留配置、执行历史和动态调度计划，无需重新清配置。
- `v0.2.5` 修复通过插件管理中心重装或热更新后，Vue 页面 API 仍绑定已停止旧实例、点击刷新提示“插件正在停止”的问题；旧接口会安全转交给 MoviePilot 当前运行实例，没有新实例时继续拒绝执行，避免旧版本恢复运行。
- `v0.2.4` 配置页会自动读取并隐藏显示 MoviePilot 的 `si-qi.xyz` 站点 Cookie，点眼睛可查看；未手动修改时继续保持自动同步，避免把自动值固化为旧 Cookie。状态页顶部的“执行”按钮已移除，并隐藏炼造上限为 0 和材料不足的重复提示。
- `v0.2.3` 修复搬砖成功 50 次后又误报“已达上限”为失败的问题；搬砖已满和沙滩冷却时会按真实状态显示；小版本热更新即使收到空配置或配置代号损坏，也会恢复 MoviePilot 已保存配置并保留执行历史和动态调度计划。
- `v0.2.2` 增加与 Vue-农场一致的可隐藏的手动 Cookie 输入框：填写后优先使用手动 Cookie，清空后恢复 MoviePilot 站点自动同步；同时修复更新后一直提示“插件正在停止”、搬砖等手动任务无法执行的问题。
- `v0.2.1` 修复真实页面解析失败后把魔力、魔丸、搬砖、沙滩、库存和配方错误保存为全零的问题；补充服务器时间、拖拽搬砖状态和启动时沙滩补跑识别，到点任务不会再直接跳到下一天。
- 状态页和配置页统一使用 Vue-农场同款自适应主题，减少固定橙色和深色卡片；`v0.2.x` 小版本更新保留现有配置、执行历史和动态调度计划，无需重新清配置。
- 站点 Cookie 默认自动读取 MoviePilot 站点管理中的最新值；配置页保存值只在站点读取失败时作为备用，状态页只显示来源，不显示 Cookie 内容。
- 动态沙滩会按页面返回的真实冷却时间安排任务；搬砖使用独立 Cron 设置，两项任务互不占用对方的时间表。
- 页面展示真实 6 段配方链 `4/4/2/2/2/1`；网站以后调整配方时，以页面真实配方为准，不按插件里的旧固定数字硬算。
- 支持自动炼造和按网站单次上限分批兑换；默认保留 10 个魔丸，避免自动兑换后库存清空。
- 手动赠送需二次确认，减少 UID 或数量填错造成的误送；赠礼统计可分别查看赠出、收到、用户汇总和物品汇总。
- 保存配置或 MoviePilot 重启后会自动补跑已经到时间的搬砖或沙滩任务，尽量避免错过执行窗口。
- 仅首次从 `v0.1.x` 更新到本次完整重写的 `v0.2.0` 后需手动重启一次 MoviePilot，重启后会一次性重置旧配置、执行历史和动态调度计划；后续 `v0.2.x` 更新会保留配置、执行历史和动态调度计划。新版默认关闭，请先检查并保存设置，再手动启用任务。
- 插件不再提供强制 IPv4 设置，站点连接由系统自动选择可用的 IPv4 或 IPv6。

### 🧸 Vue-玩偶
- `v0.2.11` 修复 MoviePilot 容器更新后后台插件同步会再次初始化插件，Vue-玩偶停止旧实例时误删全局动态任务的问题；现在只清理插件自己的临时任务，由 MoviePilot 统一替换总调度任务，即使后续同步流程中断也不会让收回和展出任务消失。小版本升级继续保留配置、Cookie、执行历史数据和动态调度计划。
- `v0.2.10` 修复 MoviePilot 启动时插件清理旧任务会提前创建总调度器、导致 Vue-玩偶 尚未加载完成就漏注册的问题；容器重启或更新后会正常注册初始化任务，再按收回、自家展位优先和外展保护规则补跑。小版本升级继续保留配置、Cookie、执行历史数据和动态调度计划。
- `v0.2.9` 修复 MoviePilot 容器重启或更新后，启动初始化和动态展位任务可能因为调度器启动较慢而被当成已经错过的问题；迟到的任务仍会恢复刷新状态，再沿用原有收回、自家展位优先和外展保护规则判断是否补跑。小版本升级继续保留配置、Cookie、执行历史数据和动态调度计划。
- `v0.2.8` Cookie 配置改为 `Vue-思齐农场` 同款紧凑密码输入框，查看和站点同步按钮都放在输入框内；插件继续自动同步 MoviePilot 的 `si-qi.xyz` 站点 Cookie，读取失败时才使用保存值作为备用。小版本升级继续保留配置、Cookie、执行历史数据和动态调度计划。
- `v0.2.7` 为“我的外展记录”增加收回按钮：外展中的拾荒者等玩偶可以直接提前收回，完成后显示收回玩偶；复用现有收回接口并补充加载状态，继续适配浅色、深色主题和手机布局。小版本更新继续保留配置、Cookie、执行历史数据和动态调度计划。
- `v0.2.6` 将任务卡标题改为“动态运行”，去掉重复的固定周期说明和“已启用”徽标，只保留自家展位保护提示及计划时间；回收弹窗使用不透明的主题表面和更清晰的边框阴影，解决遮罩下文字、输入框和按钮过暗的问题，并继续适配浅色、深色主题。小版本更新继续保留配置、Cookie、执行历史数据和动态调度计划。
- `v0.2.5` 将动态任务改成单行下次运行时间卡，盲盒商店和我的盲盒上移到顶部；页面按我的展柜、抢占他人展位、我的外展记录、最新操作记录顺序展示，并去掉执行历史显示。玩偶柜新增闲置玩偶回收按钮，可选择数量并由后端再次校验，避免误回收展出中或冷却中的玩偶；小版本更新继续保留配置、Cookie、执行历史数据和动态调度计划。
- `v0.2.4` 将状态页进一步对齐 Vue-农场、Vue-魔丸：桌面端使用同款左右分区，动态任务与自己展位同排，寻找外展位与外展记录同排；任务时间不再跟随 MoviePilot 主题主色，浅色、深色和手机页面保持一致；小版本更新继续保留配置、Cookie、执行历史和动态调度计划。
- `v0.2.3` 进一步统一 Vue-农场、Vue-魔丸的完整页面骨架：状态页使用同款四列概览和卡片层级，配置页使用同款三列表单与开关布局，并补齐浅色、深色和手机响应式细节；小版本更新继续保留配置、Cookie、执行历史和动态调度计划。
- `v0.2.2` 将状态页和配置页的渐变外壳、半透明卡片、标题栏、开关和主题变量直接与 Vue-农场、Vue-魔丸统一，修复浅色及深色主题下层次和底色不一致；小版本更新继续保留配置、Cookie、执行历史和动态调度计划。
- `v0.2.1` 修复自己展位数量被旧页面统计覆盖的问题，空展位会正确显示为 `0/3`；小版本更新继续保留配置、Cookie、执行历史和动态调度计划。
- 状态页和配置页使用与 Vue-农场、Vue-魔丸相同的思齐农场主题自适应风格，保留盲盒、手动收回、自己展位、外展和执行历史。
- 自动任务固定先收回到期玩偶、补满自己展位，再使用剩余玩偶寻找外展位。
- 新增默认 1 小时的自家展位保护；自己展位快到收回时间且有可用玩偶时暂停外展，到点后优先收回并补满自己家。
- 外展限速为每轮最多随机检查 3 个目标，没有空位时 10 分钟后再试，避免短时间反复请求和任务刷屏。
- 默认从 MoviePilot 站点管理同步 Cookie，并保留隐藏显示和手动备用；不再限制网络协议，由系统自动选择可用的 IPv4 或 IPv6。
- 更新到 `v0.2.0` 会保留现有配置、执行历史和动态调度计划，旧接口（包括旧页面接口）也会自动转交给当前运行的新实例。

### 🎭 Vue-表情
- `v0.2.3` 修复 MoviePilot 容器更新后后台插件同步会再次初始化插件，Vue-表情停止旧实例时误删主动态任务和自动挖角任务的问题；现在只清理插件自己的临时任务，由 MoviePilot 统一替换总调度任务，即使后续同步流程中断也不会让任务消失。小版本更新继续保留配置、Cookie、历史和动态调度计划。
- `v0.2.2` 修复 MoviePilot 启动时插件清理旧任务会提前创建总调度器、导致 Vue-表情 尚未加载完成就漏注册的问题；容器重启或更新后会正常注册主初始化和自动挖角任务，再按老虎机、开包、舞台和挖角规则补跑。小版本更新继续保留配置、Cookie、历史和动态调度计划。
- `v0.2.1` 补正 Vue-表情 0.2 系列版本号，并修复 MoviePilot 容器重启或更新后，启动初始化、主动态任务和自动挖角任务可能因为调度器启动较慢而被当成已经错过的问题；迟到的任务仍会恢复刷新状态，再沿用原有老虎机、开包、舞台和挖角规则判断是否补跑。小版本更新继续保留配置、Cookie、历史和动态调度计划。
- `v0.2.0` 正式归档前面的整套页面与功能重构：状态页和配置页统一为 Vue-思齐农场同款自适应风格，重排老虎机、我的表情包、表情图鉴和舞台区域，接入网页操作日志、表情包合成及独立自动挖角，并继续适配浅色、深色和手机布局。大版本升级保留现有配置、Cookie、历史和动态调度计划。
- `v0.1.18` 修复 MoviePilot 容器重启或更新后，启动初始化、主动态任务和自动挖角任务可能因为调度器启动较慢而被当成已经错过的问题；迟到的任务仍会恢复刷新状态，再沿用原有老虎机、开包、舞台和挖角规则判断是否补跑。小版本更新继续保留配置、Cookie、历史和动态调度计划。
- `v0.1.17` 状态页重新实现左右等高：桌面端由右侧“我的表情包”确定整组高度，左侧老虎机保持自然高度，自动挖角补齐剩余空间并把内容垂直居中；Cookie 配置改为 `Vue-思齐农场` 同款紧凑密码输入框，固定自动同步 MoviePilot 站点 Cookie，读取失败时才使用保存值作为备用；手机端仍按内容自然高度排列。小版本更新继续保留配置、Cookie、历史和动态调度计划。
- `v0.1.16` 配置页的挖角演员等级多选行改为 Vue-农场偷菜作物同款紧凑布局，演员等级、检查时间段和检查间隔统一垂直居中，说明文字放到整行下方，选择多个等级时不再拉乱相邻输入框。小版本更新继续保留配置、Cookie、历史和动态调度计划。
- `v0.1.15` 状态页的自动挖角与我的表情包卡片改为按内容自然收口，不再为了左右等高保留大块底部空白；桌面与手机布局继续自适应。小版本更新继续保留配置、Cookie、历史和动态调度计划。
- `v0.1.14` 微调状态页布局：自动挖角移动到表情老虎机下方并精简为必要状态信息，与我的表情包保持左右等高布局；配置页挖角检查时间段输入值居中显示。小版本更新继续保留配置、Cookie、历史和动态调度计划。
- `v0.1.13` 新增独立自动挖角任务，默认关闭；可自行选择演员等级、检查时间段、检查间隔和每轮访问人数，默认在 07:00-23:00 每 30 分钟访问 10 人，只按网站返回的 `can_steal` 判断并按实际挖到的演员等级记录结果；小版本更新继续保留配置、Cookie、历史和动态调度计划。
- `v0.1.12` 调整我的表情包合成行：将“每次……”说明文字上移到操作行左侧，把“目标数”、数量框和合成按钮统一靠右并与说明同一行，减少卡片空白；小版本更新继续保留配置、Cookie、历史和动态调度计划。
- `v0.1.11` 恢复舞台效果的等级选择卡片，保留原有选择和演出功能但移除无法与网页一致的动画预览；收紧我的表情包合成目标数输入框并将操作区靠右，说明文字贴近操作行；小版本更新继续保留配置、Cookie、历史和动态调度计划。
- `v0.1.10` 修复网页操作日志读取：优先使用页面内嵌的 `SIQI_EMOJI_DATA.logs`，并兼容旧版日志容器格式；小版本更新继续保留配置、Cookie、历史和动态调度计划。
- `v0.1.9` 继续贴近 Vue-思齐农场的自适应风格：老虎机与我的表情包改为左右布局，桌面端约三分之一 / 三分之二，手机端自动上下排列；图鉴和初级至十级舞台中的表情进一步缩小，移除舞台效果预览与动画但保留演出功能；操作日志只从网页真实日志容器强定位读取，避免整页表情干扰；小版本更新不清除配置、Cookie、历史和动态调度计划。
- `v0.1.7` 进一步统一状态页和配置页风格：顶部概览改为 Vue-农场、Vue-魔丸、Vue-玩偶同款四色半透明卡片，动态任务、表情包、图鉴、舞台和执行历史改为一致的紧凑彩色标题栏；浅色、深色双主题和手机页面继续自适应，小版本更新保留配置、Cookie、执行历史和动态调度计划。
- `v0.1.6` 移除“优先 IPv4”设置和持续全局网络改写，站点连接由系统自动选择可用的 IPv4 或 IPv6；热更新时会识别并清理旧版遗留的 IPv4-only 选择器，旧 `force_ipv4` 配置自动忽略，小版本更新继续保留配置、Cookie、执行历史和动态调度计划。
- `v0.1.5` 将网络重试统一限制为单层最多 5 次，并为老虎机、开包、合成和舞台操作增加状态确认，避免网络超时后重复扣次数或魔力；舞台倒计时和数字操作区同步优化，小版本更新继续保留配置、执行历史与动态调度。
- `v0.1.4` 将状态页和配置页统一为 Vue-农场、Vue-魔丸、Vue-玩偶同款自适应页面骨架，浅色、深色和手机页面保持一致；小版本更新保留配置、游戏功能、执行历史与动态调度，无需重新清配置。
- 老虎机、开包、舞台演出、网页操作日志。
- 支持保存配置、启动初始化和预刷新后的自动补跑，覆盖待收开包、可开包和舞台召回 / 演出场景。

### 📊 Vue-面板
- `v0.1.39` 修复 MoviePilot 启动和插件热重载时 Vue-面板提前创建总调度器、导致部分插件定时服务偶发漏注册的问题；现在会等待 MoviePilot 统一注册任务，并继续保留配置、功能卡片、Cookie、Cron、日志和运行时间。
- `v0.1.38` 修复插件市场更新后前端已经是新版、后端仍停留在旧实例的问题；状态页会比较前后端版本，发现不一致时自动安全重载并保留全部卡片、Cookie、Cron、日志和运行时间，自动处理失败时也会提供手动重载按钮。
- `v0.1.37` 功能卡片遇到网络失败时会每隔 30 秒自动重试，最多额外重试 3 次；配置缺失、登录失效、今日已完成和证书校验失败等情况不会重复等待。
- `v0.1.36` 移除隐藏配置和后端请求中的强制 IPv4 限制，连接由系统自动选择可用的 IPv4 或 IPv6；热更新不需要重置插件，原有功能卡片、Cookie、执行历史和调度计划继续保留。
- 个人用模块化面板。
- 新增功能卡片开发说明见 `plugins.v2/vuepanel/ADD_CARD_GUIDE.md`。

### 🎞️ 飞牛影视媒体库封面生成
- 生成静态封面并回写飞牛影视。

## 使用方式

1. 在 MoviePilot 中添加自定义插件仓库：`https://github.com/lucku88/MoviePilot-Plugins/`
2. 发布新版本后，用户必须在 MoviePilot 中手动刷新插件市场。
3. 用户还必须手动安装，或手动更新 `Vue-魔丸`；本程序不会自动安装或更新插件，也不会自动重启 MoviePilot。
4. 在插件配置页启用插件；Vue-农场、Vue-魔丸、Vue-玩偶和 Vue-表情都会默认读取 MoviePilot 站点管理中的 Cookie，输入框保存值仅作同步失败备用。
5. `Vue-农场` 固定优先使用 MoviePilot 内置 OCR 批量收菜，不需要填写 OCR 地址；识别失败时会自动转为 AI 辅助或逐坑位收菜。

## 仓库结构

```text
plugins.v2/
  fnmediacovergenerator/
  vuefarm/
  vueemoji/
  vuepanel/
  vuepill/
  vuetoy/
package.v2.json
README.md
LICENSE
```

## 说明

- 本仓库插件由个人维护。
- 站点页面或接口变更后，相关插件可能需要同步更新。
- 使用前请在 MoviePilot 中刷新插件仓库并同步最新版本。

## License

MIT
