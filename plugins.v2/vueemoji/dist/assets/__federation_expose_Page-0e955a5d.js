import { importShared } from './__federation_fn_import-b37dd681.js';
import { _ as _export_sfc } from './_plugin-vue_export-helper-c4c0bc37.js';

const Page_vue_vue_type_style_index_0_scoped_af37d8af_lang = '';

const {createElementVNode:_createElementVNode,toDisplayString:_toDisplayString,openBlock:_openBlock,createElementBlock:_createElementBlock,createCommentVNode:_createCommentVNode,createTextVNode:_createTextVNode,resolveComponent:_resolveComponent,withCtx:_withCtx,createVNode:_createVNode,createBlock:_createBlock,renderList:_renderList,Fragment:_Fragment,vModelText:_vModelText,withDirectives:_withDirectives,normalizeStyle:_normalizeStyle,normalizeClass:_normalizeClass,pushScopeId:_pushScopeId,popScopeId:_popScopeId} = await importShared('vue');


const _withScopeId = n => (_pushScopeId("data-v-af37d8af"),n=n(),_popScopeId(),n);
const _hoisted_1 = { class: "emoji-shell" };
const _hoisted_2 = { class: "emoji-card emoji-hero" };
const _hoisted_3 = { class: "emoji-copy" };
const _hoisted_4 = /*#__PURE__*/ _withScopeId(() => /*#__PURE__*/_createElementVNode("div", { class: "emoji-badge" }, "Vue-表情", -1));
const _hoisted_5 = { class: "emoji-title" };
const _hoisted_6 = { class: "emoji-chip-row" };
const _hoisted_7 = {
  key: 0,
  class: "emoji-chip"
};
const _hoisted_8 = { class: "emoji-chip" };
const _hoisted_9 = {
  key: 1,
  class: "emoji-chip"
};
const _hoisted_10 = { class: "emoji-chip" };
const _hoisted_11 = { class: "emoji-action-grid" };
const _hoisted_12 = { class: "emoji-stat-grid" };
const _hoisted_13 = { class: "emoji-stat-label" };
const _hoisted_14 = { class: "emoji-stat-value" };
const _hoisted_15 = {
  key: 0,
  class: "emoji-stat-desc"
};
const _hoisted_16 = {
  key: 1,
  class: "emoji-card emoji-panel emoji-panel-summary"
};
const _hoisted_17 = { class: "emoji-section-head emoji-section-head-compact" };
const _hoisted_18 = /*#__PURE__*/ _withScopeId(() => /*#__PURE__*/_createElementVNode("h2", { class: "emoji-section-title" }, "✨ 本次摘要", -1));
const _hoisted_19 = { class: "emoji-summary-list" };
const _hoisted_20 = { class: "emoji-card emoji-panel emoji-panel-bags" };
const _hoisted_21 = { class: "emoji-section-head" };
const _hoisted_22 = /*#__PURE__*/ _withScopeId(() => /*#__PURE__*/_createElementVNode("h2", { class: "emoji-section-title" }, "🎒 我的表情包", -1));
const _hoisted_23 = { class: "emoji-panel-note" };
const _hoisted_24 = { key: 0 };
const _hoisted_25 = { class: "emoji-slot-inline" };
const _hoisted_26 = ["max"];
const _hoisted_27 = { class: "emoji-bag-grid" };
const _hoisted_28 = { class: "emoji-bag-count" };
const _hoisted_29 = { class: "emoji-inline-row" };
const _hoisted_30 = ["onUpdate:modelValue", "max"];
const _hoisted_31 = {
  key: 1,
  class: "emoji-upgrade-box"
};
const _hoisted_32 = { class: "emoji-upgrade-row" };
const _hoisted_33 = /*#__PURE__*/ _withScopeId(() => /*#__PURE__*/_createElementVNode("span", null, "目标数", -1));
const _hoisted_34 = ["onUpdate:modelValue", "max"];
const _hoisted_35 = { class: "emoji-upgrade-tip" };
const _hoisted_36 = {
  key: 0,
  class: "emoji-pending-panel"
};
const _hoisted_37 = { class: "emoji-section-head emoji-section-head-compact" };
const _hoisted_38 = /*#__PURE__*/ _withScopeId(() => /*#__PURE__*/_createElementVNode("h3", { class: "emoji-subsection-title" }, "📦 待处理开包结果", -1));
const _hoisted_39 = { class: "emoji-pending-meta" };
const _hoisted_40 = { class: "emoji-result-emoji" };
const _hoisted_41 = { class: "emoji-result-attr" };
const _hoisted_42 = { class: "emoji-result-owned" };
const _hoisted_43 = { class: "emoji-result-actions" };
const _hoisted_44 = { class: "emoji-card emoji-panel emoji-panel-catalog" };
const _hoisted_45 = /*#__PURE__*/ _withScopeId(() => /*#__PURE__*/_createElementVNode("div", { class: "emoji-section-head" }, [
  /*#__PURE__*/_createElementVNode("h2", { class: "emoji-section-title" }, "📖 表情图鉴"),
  /*#__PURE__*/_createElementVNode("div", { class: "emoji-panel-note" }, "按层级切换，默认省略显示，展开后支持完整浏览。")
], -1));
const _hoisted_46 = { class: "emoji-tier-tabs" };
const _hoisted_47 = ["onClick"];
const _hoisted_48 = { class: "emoji-sort-tabs" };
const _hoisted_49 = ["onClick"];
const _hoisted_50 = {
  key: 0,
  class: "emoji-empty"
};
const _hoisted_51 = { class: "emoji-actor-grid" };
const _hoisted_52 = ["disabled", "onClick"];
const _hoisted_53 = { class: "emoji-actor-main" };
const _hoisted_54 = { class: "emoji-actor-attr" };
const _hoisted_55 = { class: "emoji-actor-count" };
const _hoisted_56 = {
  key: 2,
  class: "emoji-actor-actions"
};
const _hoisted_57 = { class: "emoji-card emoji-panel emoji-panel-stage" };
const _hoisted_58 = { class: "emoji-section-head" };
const _hoisted_59 = /*#__PURE__*/ _withScopeId(() => /*#__PURE__*/_createElementVNode("h2", { class: "emoji-section-title" }, "🎭 表情演出舞台", -1));
const _hoisted_60 = { class: "emoji-stage-note" };
const _hoisted_61 = { class: "emoji-effect-grid" };
const _hoisted_62 = ["onClick"];
const _hoisted_63 = { class: "emoji-effect-title" };
const _hoisted_64 = { class: "emoji-effect-boost" };
const _hoisted_65 = { class: "emoji-effect-subline" };
const _hoisted_66 = { class: "emoji-effect-unlock" };
const _hoisted_67 = { class: "emoji-stage-toolbar" };
const _hoisted_68 = { class: "emoji-stage-toolbar-left" };
const _hoisted_69 = { key: 0 };
const _hoisted_70 = { key: 1 };
const _hoisted_71 = { class: "emoji-stage-toolbar-right" };
const _hoisted_72 = { class: "emoji-stage-rows" };
const _hoisted_73 = { class: "emoji-row-head" };
const _hoisted_74 = { class: "emoji-row-actions" };
const _hoisted_75 = {
  key: 0,
  class: "emoji-stage-slot-grid"
};
const _hoisted_76 = ["title", "onClick"];
const _hoisted_77 = {
  key: 0,
  class: "emoji-stage-slot-emoji"
};
const _hoisted_78 = {
  key: 1,
  class: "emoji-stage-slot-emoji"
};
const _hoisted_79 = {
  key: 2,
  class: "emoji-stage-slot-empty"
};
const _hoisted_80 = { class: "emoji-card emoji-panel emoji-panel-history" };
const _hoisted_81 = /*#__PURE__*/ _withScopeId(() => /*#__PURE__*/_createElementVNode("div", { class: "emoji-section-head" }, [
  /*#__PURE__*/_createElementVNode("h2", { class: "emoji-section-title" }, "🧾 执行历史")
], -1));
const _hoisted_82 = {
  key: 0,
  class: "emoji-empty"
};
const _hoisted_83 = {
  key: 1,
  class: "emoji-history-list"
};
const _hoisted_84 = { class: "emoji-history-top" };
const _hoisted_85 = { class: "emoji-history-lines" };
const _hoisted_86 = {
  key: 0,
  class: "emoji-history-next"
};

const {computed,onBeforeUnmount,onMounted,reactive,ref,watch} = await importShared('vue');


const pluginBase = '/plugin/VueEmoji';
const actorLimitStep = 48;

const _sfc_main = {
  __name: 'Page',
  props: {
  api: { type: Object, required: true },
  initialConfig: { type: Object, default: () => ({}) },
},
  emits: ['switch', 'close'],
  setup(__props, { emit }) {

const props = __props;





const loading = ref(false);
const rootEl = ref(null);
const status = reactive({ emoji_status: {}, history: [] });
const message = reactive({ text: '', type: 'success' });
const isDarkTheme = ref(false);
const nowTs = ref(Math.floor(Date.now() / 1000));
const dismissedSummaryKey = ref('');
const hiddenPendingKey = ref('');
const actorVisibleLimit = ref(actorLimitStep);
const selectedTier = ref('1');
const selectedEffect = ref('basic');
const actorSort = ref('points_desc');
const spinCount = ref('1');
const lastRunAutoRefreshTs = ref(0);
const lastTriggerAutoRefreshTs = ref(0);
const lastStageRefreshTs = ref(0);

const openCounts = reactive({});
const upgradeCounts = reactive({});
const draftMap = reactive({});

let timer = null;
let themeObserver = null;
let mediaQuery = null;
const refreshTimeouts = [];

const emoji = computed(() => status.emoji_status || {});
const statsItems = computed(() => emoji.value.stats || []);
const slotMachine = computed(() => emoji.value.slot_machine || {});
const bags = computed(() => emoji.value.bags || []);
const pendingOpen = computed(() => emoji.value.pending_open || {});
const actorTabs = computed(() => emoji.value.actor_tabs || []);
const actorsByTier = computed(() => emoji.value.actors_by_tier || {});
const effects = computed(() => emoji.value.effects || []);
const stage = computed(() => emoji.value.stage || {});
const stageRows = computed(() => emoji.value.stage_rows || []);
const historyItems = computed(() => status.history || emoji.value.history || []);
const summaryLines = computed(() => (emoji.value.summary || []).filter(Boolean));
const summaryKey = computed(() => summaryLines.value.join('||'));
const showSummary = computed(() => !!summaryLines.value.length && dismissedSummaryKey.value !== summaryKey.value);
const nextRunTs = computed(() => Number(emoji.value.next_run_ts || 0) || parseDateTime(emoji.value.next_run_time));
const nextTriggerTs = computed(() => Number(emoji.value.next_trigger_ts || 0) || parseDateTime(emoji.value.next_trigger_time));
const spinMax = computed(() => Math.max(1, Math.min(Number(slotMachine.value.remaining || 0), Number(slotMachine.value.max_batch || 1)) || 1));
const pendingKey = computed(() => JSON.stringify(pendingOpen.value || {}));
const pendingOpenVisible = computed(() => !!pendingOpen.value.items?.length && hiddenPendingKey.value !== pendingKey.value);
const selectedEffectName = computed(() => effects.value.find((item) => item.key === selectedEffect.value)?.name || '未选择');
const currentActors = computed(() => actorsByTier.value[String(selectedTier.value)] || []);
const visibleActors = computed(() => sortedActors.value.slice(0, actorVisibleLimit.value));
const hasMoreActors = computed(() => sortedActors.value.length > actorVisibleLimit.value);
const remainingActorCount = computed(() => Math.max(0, sortedActors.value.length - actorVisibleLimit.value));
const stageRemainText = computed(() => {
  const remain = Number(stage.value.remaining_end_ts || 0) - nowTs.value;
  if (Number(stage.value.remaining_end_ts || 0) > 0 && remain > 0) {
    return formatCountdown(remain)
  }
  return stage.value.remaining_text || '等待刷新'
});

const sortOptions = [
  { key: 'points_desc', label: 'P↓' },
  { key: 'points_asc', label: 'P↑' },
  { key: 'magic_desc', label: 'M↓' },
  { key: 'magic_asc', label: 'M↑' },
];

const sortedActors = computed(() => {
  const items = [...currentActors.value];
  return items.sort((left, right) => {
    const lPoints = Number(left.points || 0);
    const rPoints = Number(right.points || 0);
    const lMagic = Number(left.magic || 0);
    const rMagic = Number(right.magic || 0);
    if (actorSort.value === 'points_desc') {
      if (rPoints !== lPoints) return rPoints - lPoints
      if (rMagic !== lMagic) return rMagic - lMagic
    }
    if (actorSort.value === 'points_asc') {
      if (lPoints !== rPoints) return lPoints - rPoints
      if (lMagic !== rMagic) return lMagic - rMagic
    }
    if (actorSort.value === 'magic_desc') {
      if (rMagic !== lMagic) return rMagic - lMagic
      if (rPoints !== lPoints) return rPoints - lPoints
    }
    if (actorSort.value === 'magic_asc') {
      if (lMagic !== rMagic) return lMagic - rMagic
      if (lPoints !== rPoints) return lPoints - rPoints
    }
    return String(left.code || '').localeCompare(String(right.code || ''))
  })
});

const draftCount = computed(() => Object.keys(draftMap).length);

watch(
  bags,
  (items) => {
    items.forEach((bag) => {
      if (!openCounts[bag.tier]) openCounts[bag.tier] = '1';
      if (bag.upgrade_rule?.key && !upgradeCounts[bag.upgrade_rule.key]) {
        upgradeCounts[bag.upgrade_rule.key] = '1';
      }
    });
  },
  { immediate: true, deep: true },
);

watch(summaryKey, () => {
  loadDismissedSummaryKey();
});

watch(pendingKey, (nextKey, prevKey) => {
  if (nextKey && nextKey !== prevKey) {
    hiddenPendingKey.value = '';
  }
});

watch(effects, (items) => {
  const active = items.find((item) => item.active && item.unlocked);
  const firstUnlocked = items.find((item) => item.unlocked);
  if (items.some((item) => item.key === selectedEffect.value && item.unlocked)) {
    return
  }
  selectedEffect.value = active?.key || firstUnlocked?.key || 'basic';
}, { immediate: true, deep: true });

watch(actorTabs, (items) => {
  if (items.some((item) => String(item.tier) === String(selectedTier.value))) {
    return
  }
  selectedTier.value = String(items[0]?.tier || 1);
}, { immediate: true, deep: true });

watch([selectedTier, actorSort, currentActors], () => {
  actorVisibleLimit.value = actorLimitStep;
}, { deep: true });

watch(() => slotMachine.value.remaining, () => {
  spinCount.value = String(Math.min(normalizePositiveInt(spinCount.value, 1), spinMax.value));
}, { immediate: true });

watch(nextRunTs, (value) => {
  if (!value || value > nowTs.value) lastRunAutoRefreshTs.value = 0;
});

watch(nextTriggerTs, (value) => {
  if (!value || value > nowTs.value) lastTriggerAutoRefreshTs.value = 0;
});

watch(
  () => stage.value.remaining_end_ts,
  (value) => {
    if (!value || Number(value) > nowTs.value) lastStageRefreshTs.value = 0;
  },
);

watch(
  () => stage.value.has_active,
  (hasActive) => {
    if (hasActive) {
      clearDraft();
    }
  },
  { immediate: true },
);

function flash(text, type = 'success') {
  message.text = text;
  message.type = type;
}

function normalizePositiveInt(value, fallback = 1) {
  const parsed = Number.parseInt(value, 10);
  return Number.isFinite(parsed) && parsed > 0 ? parsed : fallback
}

function parseDateTime(value) {
  if (!value || typeof value !== 'string') return 0
  const safe = value.replace(/-/g, '/');
  const parsed = Date.parse(safe);
  return Number.isNaN(parsed) ? 0 : Math.floor(parsed / 1000)
}

function formatCountdown(totalSeconds) {
  const safe = Math.max(0, Math.floor(totalSeconds || 0));
  const hours = Math.floor(safe / 3600);
  const minutes = Math.floor((safe % 3600) / 60);
  const seconds = safe % 60;
  return `${hours}:${String(minutes).padStart(2, '0')}:${String(seconds).padStart(2, '0')}`
}

function bagCardStyle(bag) {
  const lightBagBackgrounds = {
    1: '#eef6ff',
    2: '#effff1',
    3: '#fff7e7',
    4: '#fff0f3',
  };
  const lightBagBorders = {
    1: 'rgba(52, 134, 214, 0.24)',
    2: 'rgba(39, 164, 75, 0.24)',
    3: 'rgba(214, 139, 18, 0.24)',
    4: 'rgba(207, 52, 82, 0.22)',
  };
  const darkBagBackgrounds = {
    1: 'linear-gradient(180deg, rgba(33, 43, 56, 0.98) 0%, rgba(26, 34, 44, 0.96) 100%)',
    2: 'linear-gradient(180deg, rgba(27, 47, 37, 0.98) 0%, rgba(23, 39, 31, 0.96) 100%)',
    3: 'linear-gradient(180deg, rgba(52, 40, 19, 0.98) 0%, rgba(43, 34, 17, 0.96) 100%)',
    4: 'linear-gradient(180deg, rgba(55, 28, 38, 0.98) 0%, rgba(45, 23, 31, 0.96) 100%)',
  };
  const darkBagBorders = {
    1: 'rgba(98, 160, 255, 0.28)',
    2: 'rgba(84, 211, 120, 0.22)',
    3: 'rgba(242, 192, 86, 0.22)',
    4: 'rgba(255, 124, 156, 0.2)',
  };
  const tier = Number(bag.tier || 0);
  return {
    '--bag-bg': isDarkTheme.value
      ? (darkBagBackgrounds[tier] || 'rgba(42, 34, 30, 0.96)')
      : (lightBagBackgrounds[tier] || bag.bg_color || ''),
    '--bag-border': isDarkTheme.value
      ? (darkBagBorders[tier] || 'rgba(124, 92, 255, 0.18)')
      : (lightBagBorders[tier] || 'rgba(129, 133, 164, 0.18)'),
    '--bag-badge': bag.badge_color || '',
    '--bag-muted': isDarkTheme.value ? 'rgba(248, 234, 219, 0.82)' : '',
  }
}

function stageSlotPalette(tier) {
  const lightPalettes = {
    1: { bg: '#eef6ff', border: 'rgba(94, 149, 226, 0.34)', color: '#285585' },
    2: { bg: '#effff1', border: 'rgba(90, 185, 108, 0.34)', color: '#2d6336' },
    3: { bg: '#fff7e7', border: 'rgba(226, 171, 69, 0.36)', color: '#7a5a15' },
    4: { bg: '#fff0f3', border: 'rgba(232, 109, 143, 0.34)', color: '#6f3850' },
    default: { bg: '#f7f8fc', border: 'rgba(129, 133, 164, 0.2)', color: '#4b5063' },
  };
  const darkPalettes = {
    1: { bg: 'rgba(37, 65, 102, 0.24)', border: 'rgba(112, 173, 255, 0.3)', color: '#dcecff' },
    2: { bg: 'rgba(45, 93, 57, 0.24)', border: 'rgba(109, 226, 137, 0.3)', color: '#d7ffe1' },
    3: { bg: 'rgba(98, 73, 28, 0.26)', border: 'rgba(255, 205, 109, 0.3)', color: '#ffe9b3' },
    4: { bg: 'rgba(124, 49, 84, 0.22)', border: 'rgba(255, 126, 165, 0.34)', color: '#ffd4e1' },
    default: { bg: 'rgba(255, 255, 255, 0.04)', border: 'rgba(124, 92, 255, 0.18)', color: '#f3f5ff' },
  };
  const paletteMap = isDarkTheme.value ? darkPalettes : lightPalettes;
  return paletteMap[tier] || paletteMap.default
}

function tierChipStyle(tab) {
  const palette = stageSlotPalette(Number(tab?.tier || 0));
  return {
    '--tier-chip-bg': palette.bg,
    '--tier-chip-border': palette.border,
    '--tier-chip-color': palette.color,
  }
}

function actorCardStyle(actor) {
  const palette = stageSlotPalette(Number(actor?.tier || selectedTier.value || 0));
  return {
    '--actor-card-bg': palette.bg,
    '--actor-card-border': palette.border,
    '--actor-card-color': palette.color,
  }
}

function stageSlotStyle(row, slot) {
  if (slot.filled) {
    const palette = stageSlotPalette(Number(slot.tier || 0));
    return {
      '--stage-slot-bg': palette.bg,
      '--stage-slot-border': palette.border,
      '--stage-slot-color': palette.color,
    }
  }
  if (draftMap[slotKey(slot)]) {
    const palette = stageSlotPalette(Number(draftMap[slotKey(slot)].tier || 0));
    return {
      '--stage-slot-bg': palette.bg,
      '--stage-slot-border': palette.border,
      '--stage-slot-color': palette.color,
    }
  }
  return {}
}

function stageSlotTitle(slot) {
  if (slot.filled) return slot.emoji || ''
  if (draftMap[slotKey(slot)]) return '点击撤回草拟演员'
  return '点击放置演员'
}

function showMoreActors() {
  actorVisibleLimit.value = sortedActors.value.length || actorLimitStep;
}

function collapseActors() {
  actorVisibleLimit.value = actorLimitStep;
}

function clearRefreshTimeouts() {
  while (refreshTimeouts.length) {
    const timerId = refreshTimeouts.pop();
    window.clearTimeout(timerId);
  }
}

function scheduleFollowupRefreshes() {
  clearRefreshTimeouts();
  for (const delay of [1200, 3200]) {
    const timerId = window.setTimeout(() => {
      void loadStatus(false);
    }, delay);
    refreshTimeouts.push(timerId);
  }
}

function slotKey(slot) {
  return `${slot.row_index}_${slot.slot_index}`
}

function draftUsage(code) {
  return Object.values(draftMap).filter((item) => item.code === code).length
}

function draftRemaining(actor) {
  const current = sortedActors.value.find((item) => item.code === actor) || currentActors.value.find((item) => item.code === actor);
  if (!current) return 0
  return Math.max(0, Number(current.available || 0) - draftUsage(actor))
}

function clearDraft() {
  Object.keys(draftMap).forEach((key) => delete draftMap[key]);
}

function nextEmptySlot() {
  for (const row of stageRows.value) {
    if (!row.unlocked) continue
    for (const slot of row.slots || []) {
      if (!slot.filled && !draftMap[slotKey(slot)]) {
        return slot
      }
    }
  }
  return null
}

function selectEffect(effect) {
  if (!effect.unlocked || stage.value.has_active) return
  selectedEffect.value = effect.key;
}

function pickActor(actor) {
  if (stage.value.has_active) {
    flash('当前已有演出进行中，请先收回', 'warning');
    return
  }
  if (!actor.can_place || draftRemaining(actor.code) <= 0) {
    flash('该演员当前没有可用数量', 'warning');
    return
  }
  const slot = nextEmptySlot();
  if (!slot) {
    flash('当前没有空舞台格子', 'warning');
    return
  }
  draftMap[slotKey(slot)] = {
    row_index: slot.row_index,
    slot_index: slot.slot_index,
    code: actor.code,
    emoji: actor.emoji,
    points: actor.points,
    magic: actor.magic,
  };
}

function handleStageSlot(row, slot) {
  if (slot.filled || stage.value.has_active) return
  const key = slotKey(slot);
  if (draftMap[key]) {
    delete draftMap[key];
  }
}

function fillCurrentTier() {
  if (stage.value.has_active) {
    flash('当前已有演出进行中，请先收回', 'warning');
    return
  }
  for (const actor of sortedActors.value) {
    let remain = draftRemaining(actor.code);
    while (remain > 0) {
      const slot = nextEmptySlot();
      if (!slot) return
      draftMap[slotKey(slot)] = {
        row_index: slot.row_index,
        slot_index: slot.slot_index,
        code: actor.code,
        emoji: actor.emoji,
        points: actor.points,
        magic: actor.magic,
      };
      remain -= 1;
    }
  }
}

function buildPlacements() {
  return Object.values(draftMap)
    .map((item) => ({
      row_index: Number(item.row_index || 0),
      slot_index: Number(item.slot_index || 0),
      emoji_code: item.code,
    }))
    .filter((item) => item.row_index > 0 && item.slot_index > 0 && item.emoji_code)
    .sort((left, right) => {
      if (left.row_index !== right.row_index) return left.row_index - right.row_index
      return left.slot_index - right.slot_index
    })
}

function loadDismissedSummaryKey() {
  if (typeof window === 'undefined' || !window.sessionStorage) {
    dismissedSummaryKey.value = '';
    return
  }
  dismissedSummaryKey.value = window.sessionStorage.getItem('vueemoji-dismissed-summary') || '';
}

function dismissSummary() {
  const key = summaryKey.value;
  dismissedSummaryKey.value = key;
  if (typeof window !== 'undefined' && window.sessionStorage) {
    if (key) {
      window.sessionStorage.setItem('vueemoji-dismissed-summary', key);
    } else {
      window.sessionStorage.removeItem('vueemoji-dismissed-summary');
    }
  }
}

function dismissSummaryOnExit() {
  if (showSummary.value) {
    dismissSummary();
  }
}

function closePendingPanel() {
  hiddenPendingKey.value = pendingKey.value;
}

async function loadStatus(showError = true) {
  try {
    const data = await props.api.get(`${pluginBase}/status`);
    applyStatusPayload(data || {});
    return true
  } catch (error) {
    if (showError) {
      flash(error?.message || '加载状态失败', 'error');
    }
    return false
  }
}

function applyStatusPayload(payload = {}) {
  const nextStatus = payload.status?.emoji_status || payload.emoji_status || payload;
  if (nextStatus?.stats || nextStatus?.slot_machine || nextStatus?.bags || nextStatus?.stage_rows) {
    status.emoji_status = nextStatus;
  }
  if (Array.isArray(payload.history)) {
    status.history = payload.history;
  } else if (Array.isArray(payload.status?.history)) {
    status.history = payload.status.history;
  }
}

async function maybeAutoRefreshStatus() {
  if (loading.value) return

  let shouldRefresh = false;
  if (nextRunTs.value && nowTs.value >= nextRunTs.value && nextRunTs.value !== lastRunAutoRefreshTs.value) {
    lastRunAutoRefreshTs.value = nextRunTs.value;
    shouldRefresh = true;
  }

  if (nextTriggerTs.value && nowTs.value >= nextTriggerTs.value && nextTriggerTs.value !== lastTriggerAutoRefreshTs.value) {
    lastTriggerAutoRefreshTs.value = nextTriggerTs.value;
    shouldRefresh = true;
  }

  const stageEndTs = Number(stage.value.remaining_end_ts || 0);
  if (stageEndTs && nowTs.value >= stageEndTs && stageEndTs !== lastStageRefreshTs.value) {
    lastStageRefreshTs.value = stageEndTs;
    shouldRefresh = true;
  }

  if (shouldRefresh) {
    await loadStatus(false);
  }
}

async function withAction(action, fallback, afterAction = null) {
  loading.value = true;
  try {
    const result = await action();
    applyStatusPayload(result || {});
    await loadStatus(false);
    scheduleFollowupRefreshes();
    if (afterAction) {
      afterAction(result);
    }
    flash(result?.message || fallback);
  } catch (error) {
    flash(error?.message || fallback, 'error');
  } finally {
    loading.value = false;
  }
}

function refreshData() {
  return withAction(() => props.api.post(`${pluginBase}/refresh`), '状态已刷新')
}

function runNow() {
  return withAction(() => props.api.post(`${pluginBase}/run`), '执行完成')
}

function syncCookie() {
  return withAction(() => props.api.get(`${pluginBase}/cookie`), 'Cookie 已同步')
}

function spinSlot() {
  const count = Math.min(normalizePositiveInt(spinCount.value, 1), spinMax.value);
  spinCount.value = String(count);
  return withAction(() => props.api.post(`${pluginBase}/spin`, { count }), '转动完成')
}

function openBag(bag) {
  const count = Math.min(normalizePositiveInt(openCounts[bag.tier], 1), Math.max(Number(bag.open_max || 1), 1));
  openCounts[bag.tier] = String(count);
  return withAction(() => props.api.post(`${pluginBase}/open-bag`, { tier: bag.tier, count }), '开包完成')
}

function acceptPending() {
  return withAction(
    () => props.api.post(`${pluginBase}/accept-open`),
    '已收下',
    () => {
      hiddenPendingKey.value = '';
    },
  )
}

function rerollPending() {
  return withAction(() => props.api.post(`${pluginBase}/reroll-open`), '已重开')
}

function upgradeBag(bag) {
  const rule = bag.upgrade_rule;
  if (!rule) return
  const count = Math.min(normalizePositiveInt(upgradeCounts[rule.key], 1), Math.max(Number(rule.max_times || 1), 1));
  upgradeCounts[rule.key] = String(count);
  return withAction(() => props.api.post(`${pluginBase}/upgrade-bag`, { rule_key: rule.key, times: count }), '合成完成')
}

function expandRow(row) {
  return withAction(() => props.api.post(`${pluginBase}/expand-stage-row`, { row_index: row.row_index }), '扩展完成')
}

function confirmStage() {
  const placements = buildPlacements();
  if (!placements.length) {
    flash('请先选择演员并填入舞台格子', 'warning');
    return
  }
  return withAction(
    () => props.api.post(`${pluginBase}/confirm-stage`, { effect_key: selectedEffect.value, placements }),
    '演出已开始',
    () => clearDraft(),
  )
}

function recallStage() {
  return withAction(() => props.api.post(`${pluginBase}/recall-stage`), '收回成功')
}

function closePlugin() {
  dismissSummaryOnExit();
  emit('close');
}

function switchToConfig() {
  dismissSummaryOnExit();
  emit('switch', 'config');
}

function findThemeNode() {
  let current = rootEl.value;
  while (current) {
    if (current.getAttribute?.('data-theme')) return current
    const classValue = String(current.className || '').toLowerCase();
    if (classValue.includes('theme') || classValue.includes('v-theme--') || classValue.includes('dark') || classValue.includes('light')) {
      return current
    }
    current = current.parentElement;
  }
  const bodyClass = String(document.body?.className || '').toLowerCase();
  if (document.body?.getAttribute('data-theme') || bodyClass.includes('theme') || bodyClass.includes('v-theme--') || bodyClass.includes('dark') || bodyClass.includes('light')) {
    return document.body
  }
  const rootClass = String(document.documentElement?.className || '').toLowerCase();
  if (document.documentElement?.getAttribute('data-theme') || rootClass.includes('theme') || rootClass.includes('v-theme--') || rootClass.includes('dark') || rootClass.includes('light')) {
    return document.documentElement
  }
  return null
}

function getThemeNodes() {
  return [...new Set([findThemeNode(), document.documentElement, document.body].filter(Boolean))]
}

function nodeHasDarkHint(node) {
  const themeValue = String(node?.getAttribute?.('data-theme') || '').toLowerCase();
  const classValue = String(node?.className || '').toLowerCase();
  return ['dark', 'purple', 'transparent'].includes(themeValue)
    || classValue.includes('dark')
    || classValue.includes('theme-dark')
    || classValue.includes('v-theme--dark')
}

function nodeHasLightHint(node) {
  const themeValue = String(node?.getAttribute?.('data-theme') || '').toLowerCase();
  const classValue = String(node?.className || '').toLowerCase();
  return themeValue === 'light'
    || classValue.includes('light')
    || classValue.includes('theme-light')
    || classValue.includes('v-theme--light')
}

function detectTheme() {
  const nodes = getThemeNodes();
  if (nodes.some(nodeHasDarkHint)) {
    isDarkTheme.value = true;
    return
  }
  if (nodes.some(nodeHasLightHint)) {
    isDarkTheme.value = false;
    return
  }
  isDarkTheme.value = !!window.matchMedia?.('(prefers-color-scheme: dark)').matches;
}

function bindThemeObserver() {
  detectTheme();
  if (window.MutationObserver) {
    themeObserver = new MutationObserver(detectTheme);
    getThemeNodes().forEach((node) => {
      themeObserver.observe(node, { attributes: true, attributeFilter: ['data-theme', 'class'] });
    });
  }
  if (window.matchMedia) {
    mediaQuery = window.matchMedia('(prefers-color-scheme: dark)');
    mediaQuery.addEventListener?.('change', detectTheme);
  }
}

onMounted(async () => {
  loadDismissedSummaryKey();
  bindThemeObserver();
  await loadStatus();
  timer = window.setInterval(() => {
    nowTs.value = Math.floor(Date.now() / 1000);
    void maybeAutoRefreshStatus();
  }, 1000);
});

onBeforeUnmount(() => {
  dismissSummaryOnExit();
  if (timer) window.clearInterval(timer);
  clearRefreshTimeouts();
  themeObserver?.disconnect?.();
  mediaQuery?.removeEventListener?.('change', detectTheme);
});

return (_ctx, _cache) => {
  const _component_v_btn = _resolveComponent("v-btn");
  const _component_v_alert = _resolveComponent("v-alert");

  return (_openBlock(), _createElementBlock("div", {
    ref_key: "rootEl",
    ref: rootEl,
    class: _normalizeClass(["emoji-page", { 'is-dark-theme': isDarkTheme.value }])
  }, [
    _createElementVNode("div", _hoisted_1, [
      _createElementVNode("section", _hoisted_2, [
        _createElementVNode("div", _hoisted_3, [
          _hoisted_4,
          _createElementVNode("h1", _hoisted_5, _toDisplayString(emoji.value.title || '表情演出'), 1),
          _createElementVNode("div", _hoisted_6, [
            (status.last_run && status.last_run !== '暂无')
              ? (_openBlock(), _createElementBlock("span", _hoisted_7, "最近执行 " + _toDisplayString(status.last_run), 1))
              : _createCommentVNode("", true),
            _createElementVNode("span", _hoisted_8, "下次运行 " + _toDisplayString(emoji.value.next_run_time || status.next_run_time || '等待刷新'), 1),
            (emoji.value.next_trigger_time || status.next_trigger_time)
              ? (_openBlock(), _createElementBlock("span", _hoisted_9, "计划触发 " + _toDisplayString(emoji.value.next_trigger_time || status.next_trigger_time), 1))
              : _createCommentVNode("", true),
            _createElementVNode("span", _hoisted_10, _toDisplayString(emoji.value.cookie_source || status.cookie_source || '未同步'), 1)
          ])
        ]),
        _createElementVNode("div", _hoisted_11, [
          _createVNode(_component_v_btn, {
            color: "success",
            variant: "flat",
            loading: loading.value,
            onClick: runNow
          }, {
            default: _withCtx(() => [
              _createTextVNode("立即执行")
            ]),
            _: 1
          }, 8, ["loading"]),
          _createVNode(_component_v_btn, {
            color: "primary",
            variant: "flat",
            loading: loading.value,
            onClick: refreshData
          }, {
            default: _withCtx(() => [
              _createTextVNode("刷新状态")
            ]),
            _: 1
          }, 8, ["loading"]),
          _createVNode(_component_v_btn, {
            color: "warning",
            variant: "flat",
            loading: loading.value,
            onClick: syncCookie
          }, {
            default: _withCtx(() => [
              _createTextVNode("同步 Cookie")
            ]),
            _: 1
          }, 8, ["loading"]),
          _createVNode(_component_v_btn, {
            variant: "text",
            onClick: switchToConfig
          }, {
            default: _withCtx(() => [
              _createTextVNode("配置")
            ]),
            _: 1
          }),
          _createVNode(_component_v_btn, {
            variant: "text",
            onClick: closePlugin
          }, {
            default: _withCtx(() => [
              _createTextVNode("关闭")
            ]),
            _: 1
          })
        ])
      ]),
      (message.text)
        ? (_openBlock(), _createBlock(_component_v_alert, {
            key: 0,
            type: message.type,
            variant: "tonal"
          }, {
            default: _withCtx(() => [
              _createTextVNode(_toDisplayString(message.text), 1)
            ]),
            _: 1
          }, 8, ["type"]))
        : _createCommentVNode("", true),
      _createElementVNode("section", _hoisted_12, [
        (_openBlock(true), _createElementBlock(_Fragment, null, _renderList(statsItems.value, (item) => {
          return (_openBlock(), _createElementBlock("article", {
            key: item.label,
            class: "emoji-card emoji-stat-card"
          }, [
            _createElementVNode("div", _hoisted_13, _toDisplayString(item.label), 1),
            _createElementVNode("div", _hoisted_14, _toDisplayString(item.value), 1),
            (item.desc)
              ? (_openBlock(), _createElementBlock("div", _hoisted_15, _toDisplayString(item.desc), 1))
              : _createCommentVNode("", true)
          ]))
        }), 128))
      ]),
      (showSummary.value)
        ? (_openBlock(), _createElementBlock("section", _hoisted_16, [
            _createElementVNode("div", _hoisted_17, [
              _hoisted_18,
              _createVNode(_component_v_btn, {
                variant: "text",
                size: "small",
                onClick: dismissSummary
              }, {
                default: _withCtx(() => [
                  _createTextVNode("关闭")
                ]),
                _: 1
              })
            ]),
            _createElementVNode("div", _hoisted_19, [
              (_openBlock(true), _createElementBlock(_Fragment, null, _renderList(summaryLines.value, (line) => {
                return (_openBlock(), _createElementBlock("div", {
                  key: line,
                  class: "emoji-summary-line"
                }, _toDisplayString(line), 1))
              }), 128))
            ])
          ]))
        : _createCommentVNode("", true),
      _createElementVNode("section", _hoisted_20, [
        _createElementVNode("div", _hoisted_21, [
          _createElementVNode("div", null, [
            _hoisted_22,
            _createElementVNode("div", _hoisted_23, [
              _createTextVNode(" 今日老虎机 " + _toDisplayString(slotMachine.value.used || 0) + "/" + _toDisplayString(slotMachine.value.limit || 0) + " ", 1),
              (slotMachine.value.base || slotMachine.value.extra)
                ? (_openBlock(), _createElementBlock("span", _hoisted_24, " （基础" + _toDisplayString(slotMachine.value.base || 0) + " + f(hnr*等级) " + _toDisplayString(slotMachine.value.extra || 0) + "） ", 1))
                : _createCommentVNode("", true)
            ])
          ]),
          _createElementVNode("div", _hoisted_25, [
            _withDirectives(_createElementVNode("input", {
              "onUpdate:modelValue": _cache[0] || (_cache[0] = $event => ((spinCount).value = $event)),
              class: "emoji-number-input",
              type: "number",
              min: "1",
              max: Math.max(spinMax.value, 1)
            }, null, 8, _hoisted_26), [
              [_vModelText, spinCount.value]
            ]),
            _createVNode(_component_v_btn, {
              color: "deep-orange",
              variant: "flat",
              loading: loading.value,
              disabled: loading.value || !slotMachine.value.remaining,
              onClick: spinSlot
            }, {
              default: _withCtx(() => [
                _createTextVNode(" 转动 ")
              ]),
              _: 1
            }, 8, ["loading", "disabled"])
          ])
        ]),
        _createElementVNode("div", _hoisted_27, [
          (_openBlock(true), _createElementBlock(_Fragment, null, _renderList(bags.value, (bag) => {
            return (_openBlock(), _createElementBlock("article", {
              key: bag.tier,
              class: "emoji-card emoji-bag-card",
              style: _normalizeStyle(bagCardStyle(bag))
            }, [
              (bag.bg_image)
                ? (_openBlock(), _createElementBlock("div", {
                    key: 0,
                    class: "emoji-bag-hero",
                    style: _normalizeStyle({ backgroundImage: `url(${bag.bg_image})` })
                  }, null, 4))
                : _createCommentVNode("", true),
              _createElementVNode("div", {
                class: "emoji-bag-name",
                style: _normalizeStyle({ color: bag.badge_color || '' })
              }, _toDisplayString(bag.name), 5),
              _createElementVNode("div", _hoisted_28, "持有 " + _toDisplayString(bag.quantity), 1),
              _createElementVNode("div", _hoisted_29, [
                _withDirectives(_createElementVNode("input", {
                  "onUpdate:modelValue": $event => ((openCounts[bag.tier]) = $event),
                  class: "emoji-number-input",
                  type: "number",
                  min: "1",
                  max: Math.max(bag.open_max || 1, 1)
                }, null, 8, _hoisted_30), [
                  [_vModelText, openCounts[bag.tier]]
                ]),
                _createVNode(_component_v_btn, {
                  color: "deep-orange",
                  variant: "flat",
                  loading: loading.value,
                  disabled: loading.value || !bag.can_open,
                  onClick: $event => (openBag(bag))
                }, {
                  default: _withCtx(() => [
                    _createTextVNode(" 开包 ")
                  ]),
                  _: 2
                }, 1032, ["loading", "disabled", "onClick"])
              ]),
              (bag.upgrade_rule)
                ? (_openBlock(), _createElementBlock("div", _hoisted_31, [
                    _createElementVNode("div", _hoisted_32, [
                      _hoisted_33,
                      _withDirectives(_createElementVNode("input", {
                        "onUpdate:modelValue": $event => ((upgradeCounts[bag.upgrade_rule.key]) = $event),
                        class: "emoji-number-input",
                        type: "number",
                        min: "1",
                        max: Math.max(bag.upgrade_rule.max_times || 1, 1)
                      }, null, 8, _hoisted_34), [
                        [_vModelText, upgradeCounts[bag.upgrade_rule.key]]
                      ]),
                      _createVNode(_component_v_btn, {
                        color: "amber-darken-2",
                        variant: "flat",
                        loading: loading.value,
                        disabled: loading.value || !bag.upgrade_rule.enabled,
                        onClick: $event => (upgradeBag(bag))
                      }, {
                        default: _withCtx(() => [
                          _createTextVNode(" 合成 ")
                        ]),
                        _: 2
                      }, 1032, ["loading", "disabled", "onClick"])
                    ]),
                    _createElementVNode("div", _hoisted_35, _toDisplayString(bag.upgrade_rule.tip), 1)
                  ]))
                : _createCommentVNode("", true)
            ], 4))
          }), 128))
        ]),
        (pendingOpenVisible.value && pendingOpen.value.items?.length)
          ? (_openBlock(), _createElementBlock("article", _hoisted_36, [
              _createElementVNode("div", _hoisted_37, [
                _hoisted_38,
                _createVNode(_component_v_btn, {
                  variant: "text",
                  size: "small",
                  onClick: closePendingPanel
                }, {
                  default: _withCtx(() => [
                    _createTextVNode("关闭")
                  ]),
                  _: 1
                })
              ]),
              _createElementVNode("div", _hoisted_39, [
                _createElementVNode("span", null, _toDisplayString(pendingOpen.value.bag_name) + " x" + _toDisplayString(pendingOpen.value.bag_count), 1),
                _createElementVNode("span", null, "已重开 " + _toDisplayString(pendingOpen.value.reroll_count || 0) + " 次", 1),
                _createElementVNode("span", null, "下次重开消耗 " + _toDisplayString(pendingOpen.value.next_reroll_cost || 0) + " 魔力", 1)
              ]),
              _createElementVNode("div", {
                class: _normalizeClass(["emoji-result-grid", { single: pendingOpen.value.items.length === 1 }])
              }, [
                (_openBlock(true), _createElementBlock(_Fragment, null, _renderList(pendingOpen.value.items, (item, index) => {
                  return (_openBlock(), _createElementBlock("article", {
                    key: `pending-${index}`,
                    class: "emoji-result-item"
                  }, [
                    _createElementVNode("div", _hoisted_40, _toDisplayString(item.emoji), 1),
                    _createElementVNode("div", _hoisted_41, "P" + _toDisplayString(item.points) + " · M" + _toDisplayString(item.magic), 1),
                    _createElementVNode("div", _hoisted_42, "已有 " + _toDisplayString(item.owned_count), 1)
                  ]))
                }), 128))
              ], 2),
              _createElementVNode("div", _hoisted_43, [
                _createVNode(_component_v_btn, {
                  color: "primary",
                  variant: "flat",
                  loading: loading.value,
                  onClick: rerollPending
                }, {
                  default: _withCtx(() => [
                    _createTextVNode("重开")
                  ]),
                  _: 1
                }, 8, ["loading"]),
                _createVNode(_component_v_btn, {
                  color: "success",
                  variant: "flat",
                  loading: loading.value,
                  onClick: acceptPending
                }, {
                  default: _withCtx(() => [
                    _createTextVNode("收下")
                  ]),
                  _: 1
                }, 8, ["loading"])
              ])
            ]))
          : _createCommentVNode("", true)
      ]),
      _createElementVNode("section", _hoisted_44, [
        _hoisted_45,
        _createElementVNode("div", _hoisted_46, [
          (_openBlock(true), _createElementBlock(_Fragment, null, _renderList(actorTabs.value, (tab) => {
            return (_openBlock(), _createElementBlock("button", {
              key: tab.tier,
              type: "button",
              class: _normalizeClass(["emoji-tier-chip", { active: String(selectedTier.value) === String(tab.tier) }]),
              style: _normalizeStyle(tierChipStyle(tab)),
              onClick: $event => (selectedTier.value = String(tab.tier))
            }, _toDisplayString(tab.name) + " " + _toDisplayString(tab.owned) + "/" + _toDisplayString(tab.total || '❓'), 15, _hoisted_47))
          }), 128))
        ]),
        _createElementVNode("div", _hoisted_48, [
          (_openBlock(), _createElementBlock(_Fragment, null, _renderList(sortOptions, (item) => {
            return _createElementVNode("button", {
              key: item.key,
              type: "button",
              class: _normalizeClass(["emoji-sort-chip", { active: actorSort.value === item.key }]),
              onClick: $event => (actorSort.value = item.key)
            }, _toDisplayString(item.label), 11, _hoisted_49)
          }), 64))
        ]),
        (!currentActors.value.length)
          ? (_openBlock(), _createElementBlock("div", _hoisted_50, "当前层级暂无可用演员"))
          : (_openBlock(), _createElementBlock("div", {
              key: 1,
              class: _normalizeClass(["emoji-actor-scroll", { expanded: actorVisibleLimit.value >= sortedActors.value.length }])
            }, [
              _createElementVNode("div", _hoisted_51, [
                (_openBlock(true), _createElementBlock(_Fragment, null, _renderList(visibleActors.value, (actor) => {
                  return (_openBlock(), _createElementBlock("button", {
                    key: actor.code,
                    type: "button",
                    class: "emoji-actor-card",
                    style: _normalizeStyle(actorCardStyle(actor)),
                    disabled: stage.value.has_active || !actor.can_place || draftRemaining(actor.code) <= 0,
                    onClick: $event => (pickActor(actor))
                  }, [
                    _createElementVNode("div", _hoisted_53, _toDisplayString(actor.emoji), 1),
                    _createElementVNode("div", _hoisted_54, "P" + _toDisplayString(actor.points) + " · M" + _toDisplayString(actor.magic), 1),
                    _createElementVNode("div", _hoisted_55, "x" + _toDisplayString(draftRemaining(actor.code)), 1)
                  ], 12, _hoisted_52))
                }), 128))
              ])
            ], 2)),
        (hasMoreActors.value || actorVisibleLimit.value > actorLimitStep)
          ? (_openBlock(), _createElementBlock("div", _hoisted_56, [
              (hasMoreActors.value)
                ? (_openBlock(), _createBlock(_component_v_btn, {
                    key: 0,
                    variant: "tonal",
                    color: "primary",
                    size: "small",
                    onClick: showMoreActors
                  }, {
                    default: _withCtx(() => [
                      _createTextVNode(" 显示全部剩余 " + _toDisplayString(remainingActorCount.value) + " 个 ", 1)
                    ]),
                    _: 1
                  }))
                : _createCommentVNode("", true),
              (actorVisibleLimit.value > actorLimitStep)
                ? (_openBlock(), _createBlock(_component_v_btn, {
                    key: 1,
                    variant: "text",
                    size: "small",
                    onClick: collapseActors
                  }, {
                    default: _withCtx(() => [
                      _createTextVNode(" 收起 ")
                    ]),
                    _: 1
                  }))
                : _createCommentVNode("", true)
            ]))
          : _createCommentVNode("", true)
      ]),
      _createElementVNode("section", _hoisted_57, [
        _createElementVNode("div", _hoisted_58, [
          _hoisted_59,
          _createElementVNode("div", _hoisted_60, [
            _createElementVNode("span", null, "当前舞台：" + _toDisplayString(stage.value.current_effect_name || '未开始'), 1),
            _createElementVNode("span", null, _toDisplayString(stage.value.current_text || '当前无演出演员'), 1)
          ])
        ]),
        _createElementVNode("div", _hoisted_61, [
          (_openBlock(true), _createElementBlock(_Fragment, null, _renderList(effects.value, (effect) => {
            return (_openBlock(), _createElementBlock("article", {
              key: effect.key,
              class: _normalizeClass(["emoji-effect-card", {
              active: selectedEffect.value === effect.key,
              locked: !effect.unlocked,
            }]),
              onClick: $event => (selectEffect(effect))
            }, [
              _createElementVNode("div", _hoisted_63, _toDisplayString(effect.name), 1),
              _createElementVNode("div", _hoisted_64, "积分+" + _toDisplayString(effect.point_bonus_pct) + "% · 魔力+" + _toDisplayString(effect.magic_bonus_pct) + "%", 1),
              _createElementVNode("div", _hoisted_65, _toDisplayString(effect.duration_text || `${effect.duration_seconds || 0} 秒`), 1),
              _createElementVNode("div", _hoisted_66, _toDisplayString(effect.unlocked ? '已解锁' : effect.unlock_text || '未解锁'), 1)
            ], 10, _hoisted_62))
          }), 128))
        ]),
        _createElementVNode("div", _hoisted_67, [
          _createElementVNode("div", _hoisted_68, [
            (stage.value.has_active)
              ? (_openBlock(), _createElementBlock("span", _hoisted_69, "演出剩余 " + _toDisplayString(stageRemainText.value), 1))
              : (_openBlock(), _createElementBlock("span", _hoisted_70, "当前效果 " + _toDisplayString(selectedEffectName.value), 1))
          ]),
          _createElementVNode("div", _hoisted_71, [
            _createVNode(_component_v_btn, {
              color: "warning",
              variant: "flat",
              disabled: loading.value || stage.value.has_active || !sortedActors.value.length,
              onClick: fillCurrentTier
            }, {
              default: _withCtx(() => [
                _createTextVNode(" 一键放置当前层级 ")
              ]),
              _: 1
            }, 8, ["disabled"]),
            _createVNode(_component_v_btn, {
              color: "primary",
              variant: "flat",
              disabled: loading.value || !draftCount.value || stage.value.has_active,
              onClick: confirmStage
            }, {
              default: _withCtx(() => [
                _createTextVNode(" 确认演出 ")
              ]),
              _: 1
            }, 8, ["disabled"]),
            _createVNode(_component_v_btn, {
              color: "success",
              variant: "flat",
              disabled: loading.value || !stage.value.has_active,
              onClick: recallStage
            }, {
              default: _withCtx(() => [
                _createTextVNode(" 收回演出 ")
              ]),
              _: 1
            }, 8, ["disabled"])
          ])
        ]),
        _createElementVNode("div", _hoisted_72, [
          (_openBlock(true), _createElementBlock(_Fragment, null, _renderList(stageRows.value, (row) => {
            return (_openBlock(), _createElementBlock("article", {
              key: row.row_index,
              class: "emoji-row-card"
            }, [
              _createElementVNode("div", _hoisted_73, [
                _createElementVNode("div", null, [
                  _createElementVNode("strong", null, _toDisplayString(row.name), 1),
                  _createElementVNode("span", null, "（解锁声誉 " + _toDisplayString(row.unlock_points) + "）", 1)
                ]),
                _createElementVNode("div", null, _toDisplayString(row.unlocked ? `已开 ${row.slot_count}/${row.max_slots} 格` : '未解锁'), 1)
              ]),
              _createElementVNode("div", _hoisted_74, [
                (row.unlocked)
                  ? (_openBlock(), _createBlock(_component_v_btn, {
                      key: 0,
                      color: "amber-darken-2",
                      variant: "flat",
                      loading: loading.value,
                      disabled: loading.value || !row.can_expand,
                      onClick: $event => (expandRow(row))
                    }, {
                      default: _withCtx(() => [
                        _createTextVNode(" 扩展一格（" + _toDisplayString(row.next_expand_cost || 0) + "魔力） ", 1)
                      ]),
                      _: 2
                    }, 1032, ["loading", "disabled", "onClick"]))
                  : _createCommentVNode("", true)
              ]),
              (row.unlocked)
                ? (_openBlock(), _createElementBlock("div", _hoisted_75, [
                    (_openBlock(true), _createElementBlock(_Fragment, null, _renderList(row.slots, (slot) => {
                      return (_openBlock(), _createElementBlock("button", {
                        key: `${row.row_index}-${slot.slot_index}`,
                        type: "button",
                        class: _normalizeClass(["emoji-stage-slot", {
                  filled: slot.filled,
                  draft: !!draftMap[slotKey(slot)],
                  active: slot.filled,
                }]),
                        style: _normalizeStyle(stageSlotStyle(row, slot)),
                        title: stageSlotTitle(slot),
                        onClick: $event => (handleStageSlot(row, slot))
                      }, [
                        (slot.filled)
                          ? (_openBlock(), _createElementBlock("div", _hoisted_77, _toDisplayString(slot.emoji), 1))
                          : (draftMap[slotKey(slot)])
                            ? (_openBlock(), _createElementBlock("div", _hoisted_78, _toDisplayString(draftMap[slotKey(slot)].emoji), 1))
                            : (_openBlock(), _createElementBlock("div", _hoisted_79, "待定"))
                      ], 14, _hoisted_76))
                    }), 128))
                  ]))
                : _createCommentVNode("", true)
            ]))
          }), 128))
        ])
      ]),
      _createElementVNode("section", _hoisted_80, [
        _hoisted_81,
        (!historyItems.value.length)
          ? (_openBlock(), _createElementBlock("div", _hoisted_82, "暂无执行历史"))
          : (_openBlock(), _createElementBlock("div", _hoisted_83, [
              (_openBlock(true), _createElementBlock(_Fragment, null, _renderList(historyItems.value, (item) => {
                return (_openBlock(), _createElementBlock("article", {
                  key: `${item.time}-${item.title}`,
                  class: "emoji-history-item"
                }, [
                  _createElementVNode("div", _hoisted_84, [
                    _createElementVNode("strong", null, _toDisplayString(item.title || '任务结果'), 1),
                    _createElementVNode("span", null, _toDisplayString(item.time), 1)
                  ]),
                  _createElementVNode("div", _hoisted_85, _toDisplayString((item.lines || []).join(' / ')), 1),
                  (item.next_run)
                    ? (_openBlock(), _createElementBlock("div", _hoisted_86, "下次运行 " + _toDisplayString(item.next_run), 1))
                    : _createCommentVNode("", true)
                ]))
              }), 128))
            ]))
      ])
    ])
  ], 2))
}
}

};
const PageView = /*#__PURE__*/_export_sfc(_sfc_main, [['__scopeId',"data-v-af37d8af"]]);

export { PageView as default };
